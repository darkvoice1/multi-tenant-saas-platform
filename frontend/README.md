# Frontend (Vue3)

## Setup
1. Install deps
   - `npm install`
2. Run dev server
   - `npm run dev`

## Auth Demo
- Login page: `/login`
- Dashboard: `/dashboard`
- Admin page: `/admin` (role-guarded)

## Target
- TypeScript + Pinia + permission-based routing
